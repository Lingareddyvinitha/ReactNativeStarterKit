import styled from 'styled-components/native';
import tailwind from 'tailwind-rn';

export const SliderWrapper = styled.View`
  ${tailwind('justify-center flex-row align-center mr-2')}
`;

export const SliderValue = styled.Text`
  ${tailwind('')}
`;
